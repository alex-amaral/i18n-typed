import { translations } from '../locales/translations';
import { _t } from '../utils/messages';

export const messages = {
  title: translations.Home.title,
  description: _t(translations.description, 'Descrição'),
};
